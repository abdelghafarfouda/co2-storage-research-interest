# Tested with Python 3.11.15 on a CPU. These are the versions used for the last clean run.
numpy==2.4.4
scipy==1.17.1
pandas==3.0.2
scikit-learn==1.8.0
matplotlib==3.10.9
torch==2.14.0          # Notebook 03 only; the CPU-only build is enough (see README)

# To open and run the notebooks
jupyterlab
ipykernel==7.3.0
